name: Deploy Web Flasher

on:
  push:
    tags:
      - 'v*'  # Deploy on releases
    branches: [ main ]
    paths:
      - 'tools/web-flasher/**'  # Also deploy when web-flasher files change
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write
  actions: read
  deployments: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './tools/web-flasher'
          
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4